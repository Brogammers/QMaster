declare module 'react-native-snap-carousel' {
  // Declare types for Carousel component
  export const Carousel: any; // Replace 'any' with the actual type if available
  
  // Declare types for Pagination component
  export const Pagination: any; // Replace 'any' with the actual type if available
}
