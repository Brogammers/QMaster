// import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
// import { faCoffee } from '@fortawesome/free-solid-svg-icons';
// import { TextButtonProps } from '../../../types';

// export default function TextButton({ text, icon, textSize = 'text-xl', buttonColor = 'bg-blue-500', textColor = 'text-white', disabled = false, onPress, width = 'w-full', padding = 'p-4' }: TextButtonProps) {
//   return (
//     <button
//       disabled={disabled}
//       onClick={onPress}
//       className={`flex items-center justify-center mt-5 rounded-lg ${buttonColor} ${width} ${padding}`}
//     >
//       {icon && <FontAwesomeIcon icon={icon} size="1x" color="#17222D" />}
//       <p className={`${textSize} font-bold pl-3.5 ${textColor}`}>
//         {text}
//       </p>
//     </button>
//   );
// }
